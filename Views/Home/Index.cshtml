@model IEnumerable<Childrens_Toy_Store.Models.Produkty>

@{
    ViewBag.Title = "Home Page";
}
<h2>Dostępne Towary </h2>
<div class="table tg">
    @*<div class="container row">
            <div class="tg-6mcr col-lg-4">
                @Html.DisplayNameFor(model => model.obrazek)
            </div>
            <div class="tg-6mcr col-lg-2">
                @Html.DisplayNameFor(model => model.nazwa)
            </div>
            <div class="tg-6mcr col-lg-3">
                @Html.DisplayNameFor(model => model.cena)
            </div>
            <div class="tg-6mcr col-lg-3"></div>

        </div>*@

    @foreach (var item in Model)
    {
        <div class="container continum row">
            <div class="tg-mow8 col-lg-4">
                <img src="@item.obrazek" alt="@item.nazwa foto" style="position:center; height: 170px; width: 170px; background-repeat:round" />
            </div>
            <div class="tg-mow8 col-lg-2">
                @Html.DisplayFor(modelItem => item.nazwa)
            </div>
            <div class="tg-mow8 col-lg-3">
                @Html.DisplayFor(modelItem => item.cena)
            </div>
            <div class="tg-mow8  col-lg-3">
                @Html.ActionLink("Details", "Details", "Produkties", routeValues: new { id = item.Id_produktu }, htmlAttributes: new { @class = "przyciskakcept" })
                        @if (User.IsInRole("normal") || User.IsInRole("admin") || User.IsInRole("moderator"))
                        {

                            @Html.ActionLink("do koszyka", "addprodukt", "koszyk", routeValues: new { id = item.Id_produktu }, htmlAttributes: new { @class = "przyciskakcept" })
                            if (User.IsInRole("admin") || User.IsInRole("moderator"))
                            {

                                @Html.ActionLink("Edit", "Edit","Produkties", routeValues: new { id = item.Id_produktu }, htmlAttributes: new { @class = "przyciskakcept" })
                                @Html.ActionLink("Delete", "Delete","Produkties", routeValues: new { id = item.Id_produktu }, htmlAttributes: new { @class = "przyciskakcept" })
                            }
                        }
                    </div>
        </div>
    }

</div>
